# Benchmark reports

Generated reports are intentionally not checked in. A report is valid only
when `status` is `COMPLETED` and it contains measured results for every
configured workload and concurrency. Missing full-scale infrastructure is
reported as `NOT RUN` by the runner and must not be converted into a report.
